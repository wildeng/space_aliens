# frozen_string_literal: true

source 'https://rubygems.org'

ruby '3.3.0' # Specify the Ruby version you're using

gem 'gosu', '~> 1.4' # Use the latest 1.4.x version of Gosu

group :development, :test do
  gem 'pry-byebug'
  gem 'minitest', '~> 5.14' # Use the latest 5.14.x version of Minitest
end
